Required GitHub repository secrets:

- `EC2_HOST`: public IPv4 or DNS name of the EC2 instance
- `EC2_USER`: SSH user, usually `ubuntu`
- `EC2_SSH_KEY`: private SSH key with access to the EC2 instance
- `EC2_APP_DIR`: absolute path to the cloned repository on EC2, for example `/opt/ci_cd`
